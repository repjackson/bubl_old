template(name='profile')
    .ui.container
        .ui.segment
            .ui.header Your Name
            .ui.left.icon.input
                i.user.icon
                input#username(type='text' value=user.username)
            .ui.header Matched People
            each matchedUsersList
                .ui.list
                    .item
                        strong #{this.matchedUser} (#{this.length}):
                        each this.tagIntersection
                            | #{this}, &nbsp;
